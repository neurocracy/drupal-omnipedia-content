{#
/**
 * @file
 * Default theme implementation for legacy Omnipedia media group element.
 *
 * Available variables:
 *
 * - 'attributes': Attribute object for the <media-group> element.
 *
 * - 'items': One or more media items, each containing a media element render
 *   array.
 *
 * @ingroup themeable
 */
#}
<media-group{{ attributes }}>
{% for item in items %}
  {{ item }}
{% endfor %}
</media-group>
