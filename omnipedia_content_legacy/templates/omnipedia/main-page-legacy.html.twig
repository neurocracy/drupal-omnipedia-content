{#
/**
 * @file
 * Default theme implementation for legacy Omnipedia main page content.
 *
 * Available variables:
 *
 * - 'featured_article': A short snippet of the featured article content.
 *
 * - 'featured_article_title': The title of the featured article, used to
 *   resolve the URL for the '(Full article...)' link.
 *
 * - 'featured_article_media': The media to display alongside the featured
 *   article snippet.
 *
 * - 'news': The news for this day. Usually a list of several short items.
 *
 * - 'news_media': The media to display alongside the news.
 *
 * - 'news_media_caption': The caption to attach to the news media.
 *
 * @ingroup themeable
 */
#}
<main-page>
  <featured-article article="{{ featured_article_title }}" media="{{ featured_article_media }}">
{{ featured_article }}{# No indenting to prevent Markdown issues. #}
  </featured-article>

  <news media="{{ news_media }}" media-caption="{{ news_media_caption }}">
{{ news }}{# No indenting to prevent Markdown issues. #}
  </news>
</main-page>
